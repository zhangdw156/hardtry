# 已废弃：一条龙改由 run_local.sh 按序执行 scripts/*.sh。本文件仅保留作步骤参考。
# 步骤: train (SFT) → merge → eval

tasks:
  - type: command
    command: ["bash", "exps/__EXP_NAME__/scripts/train_local.sh"]
  - type: command
    command: ["bash", "exps/__EXP_NAME__/scripts/merge_swift_fsdp_local.sh"]
  - type: command
    command: ["bash", "exps/__EXP_NAME__/scripts/eval_local.sh"]
