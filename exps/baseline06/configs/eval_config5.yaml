# 核心模型与测试集
model_name: "Qwen/Qwen3-0.6B-FC"
test_category: "multi_turn_base"

# 实验过程文件存放的根目录
base_artifact_dir: "/dfs/data/work/hardtry/eval_results"
experiment_name: "baseline06_parallel_5runs"

# 结果汇总 CSV 存放的绝对路径
summary_output_dir: "/dfs/data/work/hardtry/exps/baseline06/eval5_results"

# 环境配置
venv_activate_path: "/dfs/data/uv-venv/gorilla/bin/activate"
remote_openai_tokenizer_path: "/dfs/data/models/Qwen3-0.6B"

# 并行参数
num_runs: 5
threads_per_run: 32

# OpenAI 兼容服务配置
remote_openai_base_url: "http://localhost:8000/v1"
remote_openai_api_key: "EMPTY"